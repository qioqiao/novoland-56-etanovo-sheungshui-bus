{
  "name": "novoland-bus-eta",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "update-routes": "node scripts/update-routes.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
